name: Docker CI/CD

on:
  push:
    branches: 
         -  "main"
         -  "dev" 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ ardakashutosh05 }}
        password: ${{ e88d134036f3c687dca212feb9bbc85be7bdfb19 }}

    - name: Build Docker image
      run: docker build -t ardakashutosh05/myapp .

    - name: Push Docker image
      run: docker push ardakashutosh05/myapp
